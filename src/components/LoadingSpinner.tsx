
const LoadingSpinner = () => {
	return (
		<div>
			<h2 className="text-4xl font-bold text-rose-600 animate-ping flex items-center justify-center">Loading...</h2>
		</div>
	);
};

export default LoadingSpinner;
